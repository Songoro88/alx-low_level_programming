#include "main.h"

/**
 * _abs - c absolute value of an integer computed
 * @n: the int to check
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
