#include "main.h"

/**
 * _islower - lowercase character is checked
 * @c: the character to be check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
