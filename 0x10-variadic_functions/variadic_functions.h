#ifndef _VARIADIC_H_
#define _VARIADIC_H_

int _putchar(char c);
int sum_them_all(const unsigned int i, ...);
void print_numbers(const char *separator, const unsigned int i, ...);
void print_strings(const char *separator, const unsigned int i, ...);
void print_all(const char * const format, ...);

#endif
